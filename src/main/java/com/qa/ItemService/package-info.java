/**
 * This package is used to perform logic of the application.
 * Firstly it would run the CrudForItemService class so that it identifies the Create, Read, Update and Delete controller.
 * Secondly it would run the ItemService class, this will show when the user has selected a controller for customer, 
 * it would identify the item table in the database and return what the user changes to thaty table. 
 * @author Gurprem Singh
 *
 */
package com.qa.ItemService;