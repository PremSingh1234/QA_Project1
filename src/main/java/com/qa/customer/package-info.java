/**
 * This package is used to take in a user inputs.
 * firstly it would run the CrudForCustomer class so that it identifies the Create, Read, Update and Delete controller.
 * Secondly it would run the CustomerCreateSystem class which would create each inputs for each controller, this means when a user inputs a controller it would give the next option they can input.
 * Thirdly it would run the MysqlCustomerDao class which first identifies that you would need a username and password to connect to the database and 
 * then identifies which controller it will be returning after the user input.
 * @author Gurprem Singh
 *
 */
package com.qa.customer;